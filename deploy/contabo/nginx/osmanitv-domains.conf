# Osmani TV production domains (VPS testing — Render API stays live for legacy APK).
# api.osmanitv.com   → Node API
# admin.osmanitv.com → Admin SPA + /api
# osmanitv.com       → Public landing
#
# TLS cert (SAN): /etc/letsencrypt/live/osmanitv.com/

# --- HTTP → HTTPS ---
server {
    listen 80;
    listen [::]:80;
    server_name api.osmanitv.com admin.osmanitv.com osmanitv.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# --- API ---
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name api.osmanitv.com;

    ssl_certificate     /etc/letsencrypt/live/osmanitv.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/osmanitv.com/privkey.pem;
    include /etc/nginx/snippets/osmani-ssl-params.conf;

    include /etc/nginx/snippets/osmani-node-api.conf;

    # Root must return 200 for health probes (not 302).
    location = / {
        proxy_pass http://127.0.0.1:10001/api/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# --- Admin panel ---
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name admin.osmanitv.com;

    ssl_certificate     /etc/letsencrypt/live/osmanitv.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/osmanitv.com/privkey.pem;
    include /etc/nginx/snippets/osmani-ssl-params.conf;

    root /var/www/osmani-admin-api/dist;
    index index.html;

    include /etc/nginx/snippets/osmani-node-api.conf;

    location /assets/ {
        try_files $uri =404;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}

# --- Main site ---
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name osmanitv.com;

    ssl_certificate     /etc/letsencrypt/live/osmanitv.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/osmanitv.com/privkey.pem;
    include /etc/nginx/snippets/osmani-ssl-params.conf;

    root /var/www/osmani-admin-api/deploy/contabo/public-osmanitv;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }
}
