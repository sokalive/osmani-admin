# Admin API (Express on Render or local). Copy to .env — no trailing slash.
# Production default should point at the API host, not the website runtime.
# Canonical website/WebView runtime is https://osmani-tv-web-vite.vercel.app
VITE_API_BASE_URL=https://osmani-admin-api.onrender.com
