# Admin API (Express on Render or local). Copy to .env — no trailing slash.
# Production default matches Render deployment.
VITE_API_BASE_URL=https://osmani-tv.onrender.com

# Must match server ADMIN_API_TOKEN / APP_UPDATE_ADMIN_TOKEN for Toa Kifurushi (manual subscription grant).
# VITE_ADMIN_API_TOKEN=
