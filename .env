# Leave empty so Contabo same-origin /api works, and Render static admin
# (osmani-admin-mpya) resolves at runtime to https://api.osmanitv.com/api.
# NEVER point this at osmani-admin-api.onrender.com — uploads would land on the
# wrong disk and Contabo/apps would 404 the images.
VITE_API_BASE_URL=
